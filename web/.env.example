# Server-only — used by app/api/demo/route.ts. Never prefix with NEXT_PUBLIC_.
INSFORGE_URL=
INSFORGE_API_KEY=

# The last full harvest (all sources) that /demo clones from and re-confirms
# against different source subsets. Get this from `npm run sync` in hydradb/.
DEMO_GOLDEN_RUN_ID=
